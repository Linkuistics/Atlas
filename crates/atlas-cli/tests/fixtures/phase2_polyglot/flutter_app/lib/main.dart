/// Flutter app entry — public top-level function so the dart-surface
/// analyser emits at least one binding.
void runMain() {
  // No-op runtime body. The visible binding is what the surface
  // analyser cares about.
}
