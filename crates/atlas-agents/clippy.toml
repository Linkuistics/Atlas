disallowed-methods = [
  { path = "tokio::runtime::Handle::block_on", reason = "atlas-agents is async; the sync→async boundary lives in atlas-cli's CLI entry point. Nested Handle::block_on inside a running runtime parks a worker thread and deadlocks the multi-threaded runtime." },
]
