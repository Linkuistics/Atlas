//! Salsa-backed query graph that drives Atlas's component-discovery
//! fixedpoint (L0 inputs through L9 projections).
//!
//! Implementation lands in subsequent backlog tasks; this skeleton
//! exists so the workspace builds.

pub fn placeholder() {}
