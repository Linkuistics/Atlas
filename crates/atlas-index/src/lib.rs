//! Schema and reader for the four Atlas YAMLs (`components.yaml`,
//! `components.overrides.yaml`, `external-components.yaml`,
//! `related-components.yaml`); rename-matching and merge-with-overrides.
//!
//! Implementation lands in the next backlog task; this skeleton exists
//! so the workspace builds and downstream crates can declare a path
//! dependency.

pub fn placeholder() {}
