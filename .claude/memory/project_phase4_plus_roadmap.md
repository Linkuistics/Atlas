---
name: Atlas vNext Phase 4+ roadmap (post-Phase-3)
description: Validated phase ordering after Phase 3 ships. Phases 4 + 5 + 6 + 7 SHIPPED; Phase 6 was the final deterministic-spine release; Phase 7 shipped the LLM-spine runtime per recast spec §11.1 (wiring deliverable; production prompts deferred). Phase 8 next (Cargo retirement).
type: project
originSessionId: c700e6f1-bd91-4b5d-b7e9-b6d2304c687c
---
User validated 2026-05-09 the post-Phase-3 phase ordering. Phase 4 SHIPPED 2026-05-09 as a cleanup release (internal code-quality + docs only). Phase 5 SHIPPED 2026-05-10 (atlas-contracts fold + multi-root delete). Phase 6 SHIPPED 2026-05-11 as the **final deterministic-spine release** before the LLM-spine recast. The post-Phase-6 ordering was retexted per the LLM-spine recast spec (canonical §10 in `docs/superpowers/specs/2026-05-06-atlas-system-model-design.md` and recast spec §13 in `docs/superpowers/specs/2026-05-11-atlas-llm-spine-recast-design.md`); the new sequence is Phase 7 LLM-spine runtime → Phase 8 Cargo retirement → Phase 9 (a/b/c) remaining language retirements → Phase 10 LLM-driven analyses → Phase 11 server mode + web-app subscriber.

**Why:** Phase 3 design §9.1 swept ~11 deferred candidates into "Phase 4" but only some are cleanups. Splitting them into focused phases keeps each phase tractable (Phase 3 was 14 PRs; bigger gets unwieldy). Consolidation at Phase 5 means six subsequent phases of analyser / LLM / subprocess work all benefit from the simpler single-root structure.

**How to apply:**

- **Phase 4** — *SHIPPED 2026-05-09.* 7 code/docs PRs (PR-1..PR-6 + PR-8) plus PR-0 (plan); PR-7 dropped (alias not orphan after Phase 3 PR-9 added two callers). Cumulative −1014 net LOC. Commits on main: `5e781d9` + `abb7f44` (PR-1), `d1a4378` (PR-2), `5bff442` (PR-3), `02d608d` (PR-4), `e89c55f` (PR-5), `2892a82` (PR-6), `009d7e5` (PR-8) + status commits. Status file: `docs/superpowers/plans/2026-05-09-phase4-status.md` (per-PR notes + closeout). Design spec: `docs/superpowers/specs/2026-05-09-atlas-vnext-phase4-design.md`. Phase 3 polyglot smoke test passed across every PR; LLM-call budget unchanged (cold≈40, warm/reports=0); zero new LLM call sites; six-file editorial tier preserved; `atlas-reports` stays pure-function.
- **Phase 5** — *SHIPPED 2026-05-10.* Monorepo consolidation part 1: atlas-contracts folded in-tree; multi-root machinery deleted. 7 PRs (PR-0..PR-6). Final commit: `a302ce5` (§5.X sweep) + `448d166` (closeout). Status: `docs/superpowers/plans/2026-05-10-phase5-status.md`. Design: `docs/superpowers/specs/2026-05-10-atlas-vnext-phase5-design.md`. Ravel + Ravel-Lite fold deferred to a later phase (may include Bazel migration).
- **Phase 6** — *SHIPPED 2026-05-11.* **Final deterministic-spine release** before the LLM-spine recast. 5 PRs landed (PR-0 plan + PR-2 contract rename-match owner-follows + PR-3 subsystem field overlay + PR-4 `--strict-overrides` + PR-5 closeout + canonical §4.3/§7/§8/§10 retext); PR-1 (`is_manifest_file` Makefile/shell extension) deferred to Phase 9c on polyglot-fixture pre-flight. Plan: `docs/superpowers/specs/2026-05-11-atlas-vnext-phase6-plan.md`. Status: `docs/superpowers/plans/2026-05-11-phase6-status.md`. Cumulative cold polyglot LLM-call count = 40 (calibrated baseline; warm + reports = 0 unchanged). Closes §11.2.4 canonical-design open question.
- **Phase 7** — *SHIPPED 2026-05-12 with calibration-pending follow-up.* **LLM-spine runtime** per canonical §10.7 / recast spec §11.1: 8 PRs (PR-0 plan + PR-1 atlas-agents + Tool trait + MCP server + async LlmBackend + PR-2 transcript cache + event bus + JSON-Lines subscriber + PR-3 22 tool wrappers via three parallel subagents + PR-4 agent runtime single-iteration + Lane A + PR-5 fixed-point + LLM dispatch + Lane B + PR-6 ratatui TUI + replay-from-cache + PR-7 end-to-end wiring + cross-transport parity + closeout). Plan: `docs/superpowers/specs/2026-05-12-atlas-vnext-phase7-plan.md`. Status: `docs/superpowers/plans/2026-05-12-phase7-status.md`. Brainstorm: `docs/superpowers/brainstorms/2026-05-12-atlas-vnext-phase7-brainstorm.md` (commit `f4ea770`).
  - **Wiring deliverable:** `atlas index --agent-runtime` routes through `AgentRuntime::run_workspace` via the single legal `Handle::block_on` boundary in `crates/atlas-cli/src/pipeline.rs::run_index_agent_runtime`. Default-false flag — production prompts (`PR-7-WIRES-REAL-PROMPT` stubs in dispatch.rs; `PR-7-WIRES-REAL-AUDITOR` stub in mod.rs) are NOT replaced in PR-7; the wiring is structural.
  - **Cumulative regression guard held:** polyglot smoke cold = ~40 LLM calls (calibrated baseline since Phase 6 PR-5), warm + reports = 0, across all 8 PRs. Cross-transport parity test (`polyglot_smoke_cross_transport_parity_claude_code_vs_codex`) green.
  - **DEFERRED to a Phase 7+ follow-up sprint (NOT Phase 8):** Step 7.4 Atlas-on-Atlas calibration (requires production prompts to emit ontology-shaped output); Step 7.6 subprocess `--disallowedTools` probe (requires a live MCP `serve_client` task driver); production dispatch prompt templates (`PR-7-WIRES-REAL-PROMPT`); cross-provider auditor closure (`PR-7-WIRES-REAL-AUDITOR`); per-provider `for_provider` closure wiring (today `None` → Lane B falls back to same-model auditor with `AuditDegraded`).
- **Phase 8** — Cargo retirement (first language LLM-driven). See recast spec §11.2.
- **Phase 9 (waves)** — Remaining language retirements: 9a TS/JS + Python; 9b C# + Dart; 9c Elixir + Racket + LispKit + Compose + Dockerfile + the deferred Phase 6 Make/shell classifier. See recast spec §11.3.
- **Phase 10** — LLM-driven analyses: pattern detection, fuzzy contract matching (extends Phase 6 PR-2 owner-follows with semantic similarity), qualitative modularity, confidence threshold calibration. See recast spec §11.4. Moved earlier than the pre-recast §10.9 placement.
- **Phase 11** — Server mode + web-app subscriber (file watcher, gRPC + GraphQL API, subscription primitives, reactive recomputation, WebSocket/SSE web-app subscriber to the event bus). See recast spec §11.5.
- **Deferred indefinitely** — gate/strict exit-code flags, modularity-score thresholds, upstream / subsystem-input impact variants, modularity history depth >5, per-language coupling normalisation, multi-tenant SaaS hosting.

Phase 4 PR-8 retexted the canonical system-model spec's §10 with the pre-recast ordering. Phase 6 PR-5 retexted §4.3 + §7 + §8 + §10 again per the LLM-spine recast: Phase 7 = LLM-spine runtime; Phase 8 = Cargo retirement; Phase 9 = remaining language retirements (waves); Phase 10 = LLM-driven analyses (moved earlier); Phase 11 = server mode + web-app subscriber; §10.11 "Migration from v1" renumbered to §10.12.
