commits:
  - paths: ["."]
    message: |
      Extract component-ontology and atlas-index to linkuistics/atlas-contracts

      Moves both crates and defaults/ontology.yaml to a new public
      linkuistics/atlas-contracts git repo (Apache-2.0 Cargo workspace).
      Atlas now depends on them via git deps pinned by sha in
      [workspace.dependencies]; atlas-cli, atlas-engine, and
      evaluation/harness switch to workspace = true deps.
      All 281 Atlas tests pass against the extracted versions.
