tasks:
- id: hand-author-second-golden-for-merged-monorepo
  title: Hand-author synthetic second golden for cross-target validation
  category: eval
  status: not_started
  dependencies: []
  description: |
    **Priority: MEDIUM.** Required for success criterion 6 (cross-target generalisation).

    **Context:** The merged monorepo target originally specified for this task is third-party and unavailable. Repurposed to use a synthetic second target from `~/Development/` — a multi-crate Cargo workspace structurally different from the dev-workspace root, sufficient to exercise cross-target stability.

    ## Target selection

    Pick a sibling repo under `~/Development/` that:
    - Is a multi-crate Cargo workspace (sub-carves differently from the top-level dev-workspace)
    - Has ≥ 10 components when Atlas sub-carves it
    - Is not the dev-workspace root itself (must exercise a different sub-carve path)

    Good candidates: any larger workspace-organised repo already present under `~/Development/` (e.g. Ravel, or another multi-crate workspace). Document the selection rationale in `notes.md`.

    ## Scope

    ### Golden

    - Hand-author `evaluation/goldens/<target-name>/components.golden.yaml` for the selected target.
    - Approximate size: tens of components (need not be hundreds — enough to stress the cross-target gap assertion).
    - `notes.md` documents: target selected, why it was chosen, and any boundary decisions that differ from what Atlas naturally produces.

    ### Cross-target validation

    - Run the tool against the synthetic target root; produce `evaluation/results/<date>-<target-name>.yaml`.
    - Compare metrics between the two goldens (dev-workspace and synthetic target):
      - Component coverage: both ≥ 0.9?
      - Spurious rate: both ≤ 0.1?
      - Kind accuracy: both ≥ 0.85?
      - Edge precision: both ≥ 0.8?
    - Gap between targets ≤ 5 percentage points on each axis (criterion 6).

    ### Iteration loop (if gap > 5pp)

    If the gap exceeds the bar, iterate on prompts, heuristics, or thresholds and re-run. Document changes in `evaluation/results/notes/<date>-iteration.md`.

    ## Out of scope

    - Ravel-Lite migration (separate tasks).
    - Performance optimisation.

    ## Exit criteria

    - Target selected and documented in `notes.md`.
    - Golden exists and passes all invariants.
    - Both goldens' metrics meet their individual targets AND cross-target gap ≤ 5pp.
    - Report HTML shows both targets with trend lines.
  results: |
    Repurposed in triage (2026-04-25): merged monorepo target is third-party and unavailable. Task pivoted to synthetic second target from `~/Development/` per option 2 in the blocked-cycle recommendation. Prior blocked-cycle context: `component-ontology` at `linkuistics/atlas-contracts` SHA `337c54368eb1393f2c3f8839a5e9042ce72cf964` matches Ravel-Lite's ontology surface 1:1; M2 migration is a pure import-path swap once Ravel-Lite is quiescent.
- id: verify-atlas-v1-success-criteria-end-to-end
  title: Verify Atlas v1 success criteria end-to-end
  category: eval
  status: not_started
  dependencies:
  - hand-author-second-golden-for-merged-monorepo
  description: |
    **Priority: HIGH.** Signs off v1.

    **Design reference:** §10.3 success criteria 1–6.

    ## Scope

    Execute each of the six v1 success criteria from §10.3 in order, document results in `evaluation/results/v1-signoff-<date>.md`:

    1. Run `atlas index /Users/antony/Development/` on a clean checkout. Assert coverage ≥ 0.9, spurious ≤ 0.1 against the golden.
    2. Re-run with no file changes. Assert 0 LLM calls (check the CLI's summary output) and byte-identical `components.yaml`.
    3. Touch one source file in one component. Re-run. Assert LLM call count equals exactly the affected component's `surface_of` plus its edge-neighbour count for edges. Log the numbers for future reference.
    4. Add a pin to `components.overrides.yaml` for one component (e.g., pin `role: my-custom-role`). Delete `components.yaml`. Re-run. Assert the pin appears in the regenerated `components.yaml` without human intervention.
    5. Run Ravel-Lite's `related-components` workflow against Atlas-produced YAMLs. Assert it runs to completion without schema errors.
    6. Run against the synthetic second target selected and evaluated in `hand-author-second-golden-for-merged-monorepo`. Assert coverage/spurious/kind/edge metrics are within 5 percentage points of the `~/Development/` run on each axis.

    ### Cross-plan dependency

    Criterion 5 cannot run until **Ravel-Lite's M5 migration** (`Migration M5: consume Atlas via atlas-index` in Ravel-Lite's backlog, formerly `ravel-lite-migration-m5-consume-atlas-via-atlas-index` in Atlas's) is complete. M5 lives in Ravel-Lite's plan, not Atlas's, so it cannot be expressed as a structural dependency here. Before starting this task, confirm M5 is `done` in Ravel-Lite by running:

    ```
    ravel-lite state backlog list /Users/antony/Development/Ravel-Lite/LLM_STATE/core --category migration
    ```

    If M5 is not done, criterion 5 must be skipped and recorded as "blocked on Ravel-Lite M5" in the sign-off report; the other five criteria can still proceed.

    ### Report

    - `evaluation/results/v1-signoff-<date>.md` contains each criterion, the commands run, the measured values, and pass/fail.
    - On any failure, the criterion lists the specific gap and what next steps would close it.

    ### Follow-up backlog

    - If any criterion fails by a wide margin, open a new backlog task to investigate. Do not silently move on.

    ## Out of scope

    - Everything in §10.2 (deferred).

    ## Exit criteria

    - All six criteria pass or have documented follow-up tasks (criterion 5 may be deferred pending Ravel-Lite M5).
    - Sign-off report committed to the Atlas repo.
    - An announcement-ready short summary written (optional) as `README-v1.md` documenting what Atlas v1 does and does not do.
- id: implement-agent-progress-feedback-live-tool-call-sub-line-during-long-llm-calls
  title: Implement agent progress feedback (live tool-call sub-line during long LLM calls)
  category: feature
  status: not_started
  dependencies:
  - design-agent-progress-feedback-feature-write-spec
  description: |
    **Priority: MEDIUM.** Long subcarve calls (10+ minutes against `claude -p`) currently show no intermediate signal; users cannot distinguish a stuck process from a working one.

    **Design spec:** `docs/superpowers/specs/2026-05-01-agent-progress-feedback-design.md` (committed in the same change-set that added this backlog item).

    ## What ships

    - New `AgentObserver` trait + `AgentEvent { CallStart, ToolUse, ToolResult, CallEnd }` in `atlas-llm`.
    - `ClaudeCodeBackend::call` rewritten to invoke `claude -p --output-format stream-json --verbose` and stream events to an optional observer. Non-streaming `--output-format json` path removed (Approach A from the spec).
    - `Reporter` (in `atlas-cli/src/progress.rs`) gains a third indicatif sub-line that mounts on `CallStart`, shows `↳ <Tool> <summary> · N tools` (truncated to terminal width), and unmounts on `CallEnd`. Auto-enabled when `Reporter::drawing() == true`.
    - `ObserverGuard` (Drop-based) ensures `CallEnd` always fires.
    - Stderr piped + drained in a small thread to avoid pipe-buffer deadlock.
    - Tests: stream-parser unit tests with three recorded JSONL fixtures (`simple-classify`, `subcarve-with-tools`, `error-max-turns`); Reporter rendering tests via direct `on_event` calls; one end-to-end integration test gated behind `ATLAS_LLM_RUN_CLAUDE_TESTS=1`.

    ## How to start

    The implementation work-phase that picks this up should begin by invoking the `superpowers:writing-plans` skill against the spec to produce a step-by-step implementation plan, then `superpowers:executing-plans` (or subagent-driven-development for parallel sub-tasks) to drive the build.

    ## Out of scope (deferred)

    - Recursing into Task subagent activity.
    - Parallel-call rendering (single-bar design).
    - Streaming partial assistant text.

    See spec §2 (non-goals) and §9 (open questions).
- id: design-agent-progress-feedback-feature-write-spec
  title: Design agent progress feedback feature (write spec)
  category: feature
  status: done
  dependencies: []
  description: |
    **Priority: MEDIUM.** One-shot scoping/design task for the agent-progress-feedback feature. Closed by the design spec; implementation is tracked separately as `implement-agent-progress-feedback-live-tool-call-sub-line-during-long-llm-calls`.

    ## Purpose

    Capture the architectural decisions, component design, data flow, error handling, and testing strategy needed before any code is written for the per-call agent visibility feature.

    ## Deliverable

    `docs/superpowers/specs/2026-05-01-agent-progress-feedback-design.md`.

    ## Context

    User reported that during a long subcarve run, a single 10-minute LLM call produces no intermediate signal beyond the elapsed-time tick. Heartbeat-only signal was rejected as insufficient — the user wants substantive content (recent tool calls + counters) on a third refreshed sub-line under the activity bar. Design was developed via the `superpowers:brainstorming` skill flow with the user picking each architectural option.
  results: |
    **What was done.** Worked through the `superpowers:brainstorming` flow with the user to scope, decide, and document the agent-progress-feedback feature. Final spec lives at `docs/superpowers/specs/2026-05-01-agent-progress-feedback-design.md`. The committing of that file is owed to the analyse-work phase that runs immediately after this work phase per the work-phase contract.

    **Key decisions captured in the spec.**

    1. **Surface content** = most recent tool call + running tool counter, on a single refreshed sub-line. Heartbeat-only was rejected by the user as insufficient.
    2. **Layout** = third indented sub-line in indicatif's `MultiProgress`, mounted on `CallStart`, unmounted on `CallEnd`. Sits between the existing activity bar and token gauge.
    3. **Activation** = tied to `Reporter::drawing()`. When progress is suppressed (`--progress=never`, non-TTY), no observer is attached but the streaming subprocess invocation still runs.
    4. **Architecture** = Approach A: `ClaudeCodeBackend` always uses `claude -p --output-format stream-json --verbose`; the legacy `--output-format json` path is removed. New `AgentObserver` trait + `AgentEvent { CallStart, ToolUse, ToolResult, CallEnd }` in `atlas-llm`. `Reporter` (in `atlas-cli`) implements `AgentObserver`.
    5. **Side-channel design** = agent events flow `ClaudeCodeBackend → Reporter` directly, parallel to the existing `Reporter::on_llm_call` pattern (engine-progress-events spec §6.3). They bypass the rest of the chain because they are transient telemetry; cache and budget logic still operate on the request/response pair.

    **Backlog adjustments.**

    - Added two tasks under category `feature`:
      - `design-agent-progress-feedback-feature-write-spec` — *this task; now done*.
      - `implement-agent-progress-feedback-live-tool-call-sub-line-during-long-llm-calls` — depends on the design task; not_started. Implementation work-phase should begin by invoking `superpowers:writing-plans` against the spec.

    **Verification.**

    - Spec self-review pass applied 3 inline fixes:
      1. Truncation rule clarified — ellipsis applies to the summary segment, not the whole line.
      2. Removed inaccurate "partial lines on slow links" claim — `BufReader::lines` splits on newlines and never returns partials.
      3. §6.1 concurrency wording disambiguated — distinguish indicatif draw-serialisation (no torn writes) from shared-state parallelism-safety (single bar = single in-flight call).
    - No code changes; nothing to compile or test.
    - `git status` left dirty (spec file untracked) for analyse-work to commit per the work-phase contract.

    **What this suggests next.**

    - Next work-phase task: pick `implement-agent-progress-feedback-live-tool-call-sub-line-during-long-llm-calls`. Begin by invoking `superpowers:writing-plans` against the spec to break the implementation into ordered tasks (parser unit-test scaffolding, `AgentObserver` types, `ClaudeCodeBackend` rewrite, `Reporter` sub-line, driver wiring, integration test). Then drive with `superpowers:executing-plans` or `subagent-driven-development`.
    - Pre-implementation prep that would help: capture two or three real `claude -p --output-format stream-json --verbose` transcripts against tiny prompts and check the JSONL into `crates/atlas-llm/tests/fixtures/stream/`. The unit tests are easier to write once the fixtures exist and the observed event vocabulary is concrete.

    **Open questions surfaced (deferred to implementation).**

    - Subagent recursion (Task tool) — surfaces as a single opaque tool_use/tool_result pair from the parent stream. Mentioned in spec §9.
    - Parallel LLM calls — single-bar design is not parallel-safe; spec §6.1 explains why and what would need to change.
