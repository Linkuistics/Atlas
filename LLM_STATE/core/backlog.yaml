tasks:
- id: hand-author-second-golden-for-merged-monorepo
  title: Hand-author synthetic second golden for cross-target validation
  category: eval
  status: not_started
  dependencies: []
  description: |
    **Priority: MEDIUM.** Required for success criterion 6 (cross-target generalisation).

    **Context:** The merged monorepo target originally specified for this task is third-party and unavailable. Repurposed to use a synthetic second target from `~/Development/` — a multi-crate Cargo workspace structurally different from the dev-workspace root, sufficient to exercise cross-target stability.

    ## Target selection

    Pick a sibling repo under `~/Development/` that:
    - Is a multi-crate Cargo workspace (sub-carves differently from the top-level dev-workspace)
    - Has ≥ 10 components when Atlas sub-carves it
    - Is not the dev-workspace root itself (must exercise a different sub-carve path)

    Good candidates: any larger workspace-organised repo already present under `~/Development/` (e.g. Ravel, or another multi-crate workspace). Document the selection rationale in `notes.md`.

    ## Scope

    ### Golden

    - Hand-author `evaluation/goldens/<target-name>/components.golden.yaml` for the selected target.
    - Approximate size: tens of components (need not be hundreds — enough to stress the cross-target gap assertion).
    - `notes.md` documents: target selected, why it was chosen, and any boundary decisions that differ from what Atlas naturally produces.

    ### Cross-target validation

    - Run the tool against the synthetic target root; produce `evaluation/results/<date>-<target-name>.yaml`.
    - Compare metrics between the two goldens (dev-workspace and synthetic target):
      - Component coverage: both ≥ 0.9?
      - Spurious rate: both ≤ 0.1?
      - Kind accuracy: both ≥ 0.85?
      - Edge precision: both ≥ 0.8?
    - Gap between targets ≤ 5 percentage points on each axis (criterion 6).

    ### Iteration loop (if gap > 5pp)

    If the gap exceeds the bar, iterate on prompts, heuristics, or thresholds and re-run. Document changes in `evaluation/results/notes/<date>-iteration.md`.

    ## Out of scope

    - Ravel-Lite migration (separate tasks).
    - Performance optimisation.

    ## Exit criteria

    - Target selected and documented in `notes.md`.
    - Golden exists and passes all invariants.
    - Both goldens' metrics meet their individual targets AND cross-target gap ≤ 5pp.
    - Report HTML shows both targets with trend lines.
  results: |
    **2026-05-03 work phase: setup + bug-discovery, not authoring.** dull (`~/Development/dull/`) was selected and validated as the second target — multi-language monorepo (~90 components per the user's hand-curated `.atlas/components.overrides.yaml`, spanning Rust/C#/Dart/Flutter/TypeScript/Python/Docker), structurally distinct from `~/Development/`. A smoke run completed at depth 0 (`105 components, 0 LLM calls, 7min wall`), and a calibration run at depth 1 produced identical output. Pin coverage is so dense that all classifications and subcarves short-circuit deterministically, which is functionally correct but means the LLM path was not exercised in this phase.

    ## Three blockers discovered (all filed as new backlog tasks)

    1. **`atlas init` scaffolds invalid override files.** `subsystems.overrides.yaml` and `components.overrides.yaml` shipped with `schema_version` commented out, so `atlas validate-overrides` failed immediately on a freshly-init'd repo. Fixed in this phase: `crates/atlas-cli/src/init.rs` templates now include active `schema_version: 1`; new regression test `freshly_init_overrides_pass_validate_overrides` covers the validate path.

    2. **L3/L8 prompt-token coverage bug.** `defaults/prompts/classify.md` and `subcarve.md` interpolated only catalog tokens (`COMPONENT_KINDS`, `LIFECYCLE_SCOPES`); the per-candidate fields supplied by `build_llm_inputs` and `build_subcarve_inputs` had no matching `{{TOKEN}}` placeholders and were silently dropped by `prompt::render`. The HTTP backend therefore sent classify/subcarve prompts with no candidate context. Fixed in this phase: classify.md and subcarve.md now interpolate every per-call input; `build_llm_inputs` keys upper-cased to match; bidirectional token-coverage tests added in `l3_classify.rs` and `l8_recurse.rs`. Memory `l5-l6-tests-include-template-token-coverage-check` rewritten to reflect bidirectional L3/L8 + forward-only L5/L6 state.

    3. **Three layers of silent LLM-failure absorption.** When dull's HTTP-routed config omits `params.max_tokens`, every L5/L6/L8 call errors with `LlmError::Setup`, L5 swallows the error into a `SurfaceRecord { notes: ... }`, the cache `call_count` doesn't tick (errors short-circuit before increment), and the `ComponentEntry` schema has no surface field — so the user sees `0 LLM calls, 0 errors, 0 tokens` with no observable failure. Filed as three separate backlog tasks: `validate-params-max-tokens-at-config-load-for-http-providers`, `surface-l5-l6-l8-setup-errors-instead-of-silently-swallowing`, `reject-http-providers-for-stage1surface-and-stage2edges-in-backendrouter`.

    ## Other work landed in this phase

    - Memory entry on `AnthropicHttpBackend` corrected — the prior note claimed prompt caching was enabled; verified that no `cache_control` is set on the request body. Updated to reflect actual state.
    - Backlog task `enable-anthropic-prompt-caching-in-anthropichttpbackend` added with the prefix-cache design (split static prefix at `{{COMPONENT_KINDS}}`/`{{LIFECYCLE_SCOPES}}` boundaries, mark `cache_control: ephemeral`, ~1024-token minimum).
    - Backlog task `prompt-template-token-coverage-check-build-time` rewritten — inverse-direction failure mode is now first-class (was punted as "harmless"); priority bumped LOW → MEDIUM.

    ## Status of this task

    **Blocked**, not failed. The exit-criteria work (golden authoring + metric scoring + cross-target gap) is unblocked once the three discovered bugs land — the architectural prerequisites are now visible. Without them, depth-1+ runs on dull silently no-op on the LLM path; cross-target metrics would be meaningless. Recommend: do `validate-params-max-tokens-at-config-load-for-http-providers` (HIGH) first, then re-run dull at depth 1 with a real LLM budget (~$5–15) to populate L5 surfaces and L6 edges, then author the golden against actual outputs.

    ## Source-file changes left for analyse-work to commit

    - `crates/atlas-cli/src/init.rs` — schema_version active in both override templates.
    - `crates/atlas-cli/tests/init_cmd.rs` — new test `freshly_init_overrides_pass_validate_overrides`.
    - `defaults/prompts/classify.md` — added per-call `{{DIR_RELATIVE}}` / `{{RATIONALE_BUNDLE}}` / `{{MANIFEST_CONTENTS}}` interpolation.
    - `defaults/prompts/subcarve.md` — added per-call `{{COMPONENT_ID}}` / `{{COMPONENT_KIND}}` / `{{COMPONENT_PATHS}}` / `{{STRUCTURAL_SIGNALS}}` / `{{EDGE_NEIGHBOURHOOD}}` / `{{PIN_SUPPRESSED_CHILDREN}}` interpolation.
    - `crates/atlas-engine/src/l3_classify.rs` — `build_llm_inputs` keys upper-cased; bidirectional token-coverage test replacing the older forward-only render-check.
    - `crates/atlas-engine/src/l8_recurse.rs` — bidirectional token-coverage test for subcarve.

    dull's `.atlas/config.yaml` was also flipped from `claude-code/...` to `anthropic/...` defaults — that change lives outside this repo's tree.
- id: audit-atlas-cli-against-llm-friendly-cli-design-guide
  title: Audit Atlas CLI against LLM-friendly CLI design guide
  category: infra
  status: not_started
  dependencies: []
  description: |
    **Priority: MEDIUM.** Audit Atlas's CLI surface (`atlas`, all subcommands, all flags, all output formats) against the LLM-friendly CLI design guide at `~/Development/Ravel-Lite/defaults/fixed-memory/cli-tool-design.md`. Produce a punch-list of findings, prioritised by ROI per the guide's own ordering (structured output > examples in --help > actionable errors > flag consistency > exit codes > defaults > non-TTY behaviour > capabilities).

    ## Why

    Atlas is increasingly invoked by other agents (Ravel-Lite tooling, future Uplift/Redeveloper consumers, ad-hoc evaluation runs in subagents). Friction in the CLI surface — missing `--json`, prose-only output, vague errors, undocumented exit codes — costs tokens and produces flaky agent loops. The guide is opinionated and ordered by impact; follow its ordering rather than re-deriving it.

    ## Scope

    ### Discovery (read-only audit)

    Walk every code path that produces user-facing output:

    - `atlas --help` and every subcommand's `--help`.
    - `atlas index` — the primary data-producing command. Outputs three YAMLs + `llm-cache.json`. Stdout summary line. Stderr progress.
    - `atlas validate-overrides` — error reporting on malformed config / overrides.
    - `atlas init` — scaffold output.
    - `atlas --version` — version output, `--json` form?
    - Error paths: missing config, missing API key, invalid model string, budget exhausted, filesystem-required prompt routed to HTTP (the new `LlmError::Setup` from `BackendRouter`), `LlmError::Invocation`, `LlmError::Schema`.
    - Exit codes: which subcommands emit which codes for which conditions.

    For each surface, score against the guide's audit checklist (lines 208–252 of `cli-tool-design.md`). Each "no" is a finding.

    ### Output

    A markdown report at `evaluation/results/cli-audit-<date>.md` with:

    - One section per audit-checklist subheading (Output, Help, Errors, Conventions, Behaviour, Discoverability).
    - Each finding tagged `[blocker]` / `[high]` / `[medium]` / `[low]` per the guide's ROI ordering.
    - A prioritised remediation backlog at the bottom — each entry small enough to be its own follow-up backlog task.
    - Explicit "out of scope for v1" calls where appropriate (e.g. `atlas capabilities` is "optional but valuable" — don't block v1 sign-off on it).

    ### Out of scope

    - Implementing fixes. This task ends at the report; remediation work spawns separate backlog tasks per the report's recommendations.
    - Auditing Ravel-Lite or other linkuistics CLIs.
    - Reworking the YAML output schemas — they are separately governed by the components/externals/related-components contracts.

    ## Exit criteria

    - `evaluation/results/cli-audit-<date>.md` exists, walks every guide audit-checklist item against Atlas's current CLI, and identifies findings.
    - Each finding is prioritised per the guide's ROI ordering (structured output > examples > errors > consistency > etc.).
    - A short follow-up backlog of 3–8 remediation tasks is proposed in the report's final section. The user reviews and decides which to file.
    - Atlas v1 sign-off (`verify-atlas-v1-success-criteria-end-to-end`) cross-references the report so anyone reading the v1 announcement understands what is and is not LLM-friendly today.

    ## References

    - Guide: `~/Development/Ravel-Lite/defaults/fixed-memory/cli-tool-design.md`. Treat the audit checklist (lines 208–252) and the anti-patterns list (lines 256–268) as load-bearing.
    - The guide's own remediation order (lines 271–284) — "Add `--json` everywhere it's missing" first, "stabilise the JSON schema" second, "rewrite error messages" third — is the recommended order for any follow-up tasks the report spawns.
- id: verify-atlas-v1-success-criteria-end-to-end
  title: Verify Atlas v1 success criteria end-to-end
  category: eval
  status: not_started
  dependencies:
  - hand-author-second-golden-for-merged-monorepo
  description: |
    **Priority: HIGH.** Signs off v1.

    **Design reference:** §10.3 success criteria 1–6.

    ## Scope

    Execute each of the six v1 success criteria from §10.3 in order, document results in `evaluation/results/v1-signoff-<date>.md`:

    1. Run `atlas index /Users/antony/Development/` on a clean checkout. Assert coverage ≥ 0.9, spurious ≤ 0.1 against the golden.
    2. Re-run with no file changes. Assert 0 LLM calls (check the CLI's summary output) and byte-identical `components.yaml`.
    3. Touch one source file in one component. Re-run. Assert LLM call count equals exactly the affected component's `surface_of` plus its edge-neighbour count for edges. Log the numbers for future reference.
    4. Add a pin to `components.overrides.yaml` for one component (e.g., pin `role: my-custom-role`). Delete `components.yaml`. Re-run. Assert the pin appears in the regenerated `components.yaml` without human intervention.
    5. Run Ravel-Lite's `related-components` workflow against Atlas-produced YAMLs. Assert it runs to completion without schema errors.
    6. Run against the synthetic second target selected and evaluated in `hand-author-second-golden-for-merged-monorepo`. Assert coverage/spurious/kind/edge metrics are within 5 percentage points of the `~/Development/` run on each axis.

    ### Cross-plan dependency

    Criterion 5 cannot run until **Ravel-Lite's M5 migration** (`Migration M5: consume Atlas via atlas-index` in Ravel-Lite's backlog, formerly `ravel-lite-migration-m5-consume-atlas-via-atlas-index` in Atlas's) is complete. M5 lives in Ravel-Lite's plan, not Atlas's, so it cannot be expressed as a structural dependency here. Before starting this task, confirm M5 is `done` in Ravel-Lite by running:

    ```
    ravel-lite state backlog list /Users/antony/Development/Ravel-Lite/LLM_STATE/core --category migration
    ```

    If M5 is not done, criterion 5 must be skipped and recorded as "blocked on Ravel-Lite M5" in the sign-off report; the other five criteria can still proceed.

    ### Report

    - `evaluation/results/v1-signoff-<date>.md` contains each criterion, the commands run, the measured values, and pass/fail.
    - On any failure, the criterion lists the specific gap and what next steps would close it.

    ### Follow-up backlog

    - If any criterion fails by a wide margin, open a new backlog task to investigate. Do not silently move on.

    ## Out of scope

    - Everything in §10.2 (deferred).

    ## Exit criteria

    - All six criteria pass or have documented follow-up tasks (criterion 5 may be deferred pending Ravel-Lite M5).
    - Sign-off report committed to the Atlas repo.
    - An announcement-ready short summary written (optional) as `README-v1.md` documenting what Atlas v1 does and does not do.
- id: enable-anthropic-prompt-caching-in-anthropichttpbackend
  title: Enable Anthropic prompt caching in AnthropicHttpBackend
  category: feature
  status: done
  dependencies: []
  description: |
    **Priority: MEDIUM.** Enable Anthropic prompt caching in `AnthropicHttpBackend` to cut input-token costs on the L3/L8/L5/L6 LLM paths.

    ## Context

    `crates/atlas-llm/src/http_anthropic.rs:84-88` currently builds a single `messages: [{role: user, content: rendered}]` body with no `cache_control` markers. A prior memory entry incorrectly claimed caching was enabled; the code does not enable it. This task closes that gap.

    OpenAI's HTTP backend (`http_openai.rs`) does **not** need code changes — OpenAI's prompt caching is automatic for prompts ≥ 1024 tokens. The Claude Code subprocess backend is out of scope for this task.

    ## What's cacheable in Atlas's prompts

    The four templates (`defaults/prompts/{classify,subcarve,stage1-surface,stage2-edges}.md`) split naturally into:

    - **Stable prefix** — the template body up to and including catalog interpolations (`{{COMPONENT_KINDS}}`, `{{LIFECYCLE_SCOPES}}`, `{{ONTOLOGY_KINDS}}`, `{{CATALOG_COMPONENTS}}`). These tokens depend only on `defaults/component-kinds.yaml` + ontology, not per-candidate inputs, so the rendered prefix is byte-identical across all calls within a run. Several hundred lines per template — well above the 1024-token Sonnet cacheable minimum.
    - **Variable suffix** — per-candidate fields (`{{COMPONENT_ID}}`, `{{COMPONENT_PATHS}}`, etc., where present) plus any per-call inputs that get interpolated.

    ## Approach

    Two implementation paths; pick during implementation:

    1. **Renderer split.** Modify `prompt::render` (or add a sibling `render_split`) to emit `(stable_prefix, variable_suffix)`. Templates declare a marker (e.g. a comment line `<!-- CACHE_BOUNDARY -->`) that splits them. `AnthropicHttpBackend` sends two content blocks:
       ```json
       "messages": [{
         "role": "user",
         "content": [
           {"type": "text", "text": "<prefix>", "cache_control": {"type": "ephemeral"}},
           {"type": "text", "text": "<suffix>"}
         ]
       }]
       ```
    2. **Two-template files.** Split each template into `<name>.prefix.md` and `<name>.suffix.md`. Renderer interpolates each; backend sends two blocks. Simpler logic in code, more files in `defaults/prompts/`.

    Recommendation: option 1. Keeps templates in one file each; explicit boundary marker is self-documenting.

    ## Pricing math

    Anthropic cache write is 1.25× normal input cost; cache read is 0.10× normal input cost. Break-even is roughly one re-use beyond the write. Atlas issues O(N) classify calls per index run where N = unpinned candidates, plus O(M) surface and one batch edge call. The catalog prefix is byte-equal across every classify call — re-use rate is high once L3 traffic is non-trivial.

    ## Out of scope

    - OpenAI HTTP backend changes (caching is automatic).
    - Claude Code subprocess caching (separate backend; subprocess CLI may not expose cache controls).
    - Cache TTL extension to 1h (default 5m is fine for Atlas's run shape).

    ## Exit criteria

    - `AnthropicHttpBackend` sends a two-block user message with `cache_control: ephemeral` on the prefix block.
    - A unit test asserts the request body shape (mock the `client.post` call or use a deserialisation round-trip).
    - A live end-to-end run on `~/Development/dull/` shows non-zero `cache_read_input_tokens` in Anthropic response usage data after the second-onwards LLM call.
    - Memory note `anthropichttpbackend-already-implemented-map-reduce-scope-is-narrower-than-task-description-implies` updated or replaced — caching is now genuinely enabled.

    ## Risks

    - Cache-key fingerprinting: `LlmFingerprint::backend_version` should incorporate "caching=on" to keep the Salsa cache sound across versions where caching changes the wire format.
    - Prefix drift: if a template-token in the cached prefix gets renamed but the marker stays put, the prefix changes silently. Add a build-time check that the rendered prefix shape matches the declared cacheable token set.
  results: |
    ## What was done

    Enabled Anthropic ephemeral prompt caching in `AnthropicHttpBackend` for the two HTTP-routable prompts (`Classify` and `Subcarve`). Stage1Surface/Stage2Edges remain claude-code-only because `BackendRouter` rejects HTTP providers for them, so they were left untouched.

    ### Implementation

    - **Renderer split** (`crates/atlas-llm/src/prompt.rs`)
      - Added `pub const CACHE_BOUNDARY_MARKER: &str = "<!-- CACHE_BOUNDARY -->"`.
      - Added `render_split(template, tokens) -> Result<(String, Option<String>), LlmError>` which renders normally then splits on the marker, dropping a single newline either side so the split is byte-clean. Returns `(rendered, None)` when the marker is absent.
      - 3 new unit tests covering marker-present, marker-absent, and marker-at-end-of-template.

    - **Template restructure** (`defaults/prompts/classify.md`)
      - Reordered sections so the stable catalogs (`{{COMPONENT_KINDS}}`, `{{LIFECYCLE_SCOPES}}`) come above the variable per-candidate inputs (`{{DIR_RELATIVE}}`, `{{RATIONALE_BUNDLE}}`, `{{MANIFEST_CONTENTS}}`). Cache boundary placed after the catalogs, before the inputs section.

    - **Cache marker** (`defaults/prompts/subcarve.md`)
      - Subcarve already had stable prose above and variable inputs below; the prompt's `Output` block was moved up alongside the prose so the marker can come before all variable interpolation. The trailing duplicate Output block was removed (single source of truth).

    - **Two-block content body** (`crates/atlas-llm/src/http_anthropic.rs`)
      - `render_request` now returns `(prefix, Option<suffix>)`.
      - New helper `build_user_content(prefix, suffix) -> Value` builds the `messages[0].content` array: when `suffix` is `Some`, two text blocks where the first carries `cache_control: {type: ephemeral}`; when `None`, a single uncached text block.
      - `call()` calls `build_user_content` and inlines the array into the request body.
      - Two new unit tests assert the request-body shape for both branches.

    - **Fingerprint bump**
      - `backend_version` is now `anthropic-http/<crate-ver>+caching=ephemeral` so the Salsa LLM-response cache invalidates on toggle. Cache soundness is preserved across the version boundary.

    ### Tests

    - `cargo test -p atlas-llm --lib` — **123 passed, 0 failed, 4 ignored**. New tests: 3 for `render_split`, 2 for `build_user_content`.
    - `cargo test --workspace` — all suites green, including `prompt_token_coverage::every_prompt_and_builder_agree_on_token_set`, `classify_prompt_token_coverage_is_bidirectional`, and `subcarve_prompt_token_coverage_is_bidirectional`. The bidirectional token-coverage tests confirm the restructured templates still satisfy the forward+inverse coverage invariant.
    - `cargo clippy -p atlas-llm --all-targets` — clean.

    ### Exit criteria status

    - ✅ `AnthropicHttpBackend` sends a two-block user message with `cache_control: ephemeral` on the prefix block (verified by unit test `build_user_content_emits_two_blocks_with_cache_control_when_split`).
    - ✅ Unit test asserts request-body shape (two new tests).
    - ❌ Live end-to-end run on `~/Development/dull/` showing non-zero `cache_read_input_tokens` in Anthropic response usage — **deferred**. Cannot perform without burning real LLM tokens; flagged as next step in the updated memory entry.
    - ✅ Memory entry `anthropichttpbackend-already-implemented-map-reduce-scope-is-narrower-than-task-description-implies` updated: title now reflects caching is enabled; body documents the implementation shape and lists live verification as a remaining step.

    ### Risks not addressed

    - The "build-time check that the rendered prefix shape matches the declared cacheable token set" listed under the task's Risks section was not implemented. It would catch silent prefix drift if a template token in the cached prefix gets renamed but the marker stays put. Token-coverage tests partly cover this (they assert every `{{TOKEN}}` is supplied), but they do not assert which tokens land on which side of the marker. Suggested follow-up: add a test that runs `render_split` and asserts a declared set of token names appear only in the prefix and another set only in the suffix.

    ### What this suggests next

    - **Live verification on dull**: route `operations.classify.model: anthropic/claude-sonnet-4-6` in the dull-workspace `.atlas/config.yaml`, run twice, observe `cache_creation_input_tokens` on the first call and `cache_read_input_tokens` on subsequent calls in the Anthropic response. Capture the figures into the memory entry.
    - **Stage1Surface / Stage2Edges**: when the filesystem-tool-use loop lands and HTTP providers can serve those prompts, this same pattern (marker + restructure if needed) should be applied to `stage1-surface.md` (currently has variables at top) and `stage2-edges.md` (already friendly: `{{ONTOLOGY_KINDS}}` near top, `{{SURFACE_RECORDS_YAML}}` at end).
- id: map-reduce-llm-architecture-with-backend-routing-for-context-light-analysis
  title: L8 map/reduce redesign and L3 HTTP routing (AnthropicHttpBackend already complete)
  category: feature
  status: not_started
  dependencies: []
  description: |
    **Priority: MEDIUM (HIGH once spike unblocks).** Foundational architectural shift for Atlas's LLM strategy. Replaces the per-component agentic-Claude-Code model with a routed two-backend design: plain Anthropic HTTP for fixed-shape classification queries, agentic Claude Code only for genuinely exploratory work. Combines this with map/reduce for L8 subcarve so a component with N immediate sub-directories costs N small parallel calls instead of one long-context-rotted agent run.

    ## Context

    Atlas's current LLM strategy:

    - Every L3, L5, and L8 prompt runs through `ClaudeCodeBackend`, which spawns a Claude Code subprocess with full tool access for each call.
    - One agent call covers the entire scope of the prompt (one component for L5, one whole subcarve decision for L8).
    - A single L8 subcarve on a real monorepo takes >15 min in observed practice (agent-progress sub-line shows extensive exploratory reads).

    Three structural problems:

    1. **Context rot.** As the agent reads files within one call, its context grows monotonically; attention smears, decisions degrade, latency climbs.
    2. **No reuse across components.** Two Rust crates with near-identical evidence each pay full agent-call cost. There is no caching across components because the per-call prompt is so large that no two prompts are byte-equal.
    3. **No parallelism.** Per-iteration loops are serial: `iter 0 · subcarve 1/74` runs to completion before 2/74 starts.

    The diagnostic lever is the agent-progress sub-line shipped in v1.2.0: when it shows the agent reading hundreds of files for one decision, the call is doing work that small fixed-shape prompts could each answer in seconds.

    ## Goal

    Re-architect Atlas's LLM dispatch with the right scope per prompt — narrow where the question is genuinely local, broad where it is genuinely cross-cutting:

    - **Per-candidate / per-component classification prompts** (L3 `is_component`, L8 per-subdir verdict): run on a plain Anthropic HTTP backend with prompt caching and structured JSON output. No subprocess, no tool use, sub-second per call.
    - **Per-component surface extraction** (L5 `Stage1Surface`): keep agentic — extracting an API surface needs to read source files, but the scope is bounded to one component's tree.
    - **Cross-component edge synthesis** (L6 `Stage2Edges`): **stays cross-component, agentic, and broadly contextual.** This is where Atlas earns its keep on tech-debt-heavy codebases — components are linked through protocols, schemas, formats, APIs, communication patterns, and endpoints, none of which are visible in any single component's surface alone. Narrowing this prompt would defeat Atlas's core value. Map/reduce simplification applies *only* to prompts whose answer is a property of one candidate; L6 synthesises a relation across many.
    - **L8 subcarve becomes map/reduce:** one HTTP call per immediate subdirectory of the component being carved (the "map"); deterministic Rust assembles per-subdir verdicts into the subcarve plan (the "reduce"). Map calls run in parallel under a bounded concurrency limit.

    Target: a 74-subcarve first-iteration on a real monorepo completes in **≤ 5 min** (current state: >>15 min), without sacrificing edge-detection quality at L6.

    ## Scope

    ### Backend routing

    - `LlmBackend` trait gains routing semantics — either a `BackendRouter` decorator that dispatches by `PromptId`, or a per-`PromptId` policy table consulted by the engine before calling.
    - Default routing reflects the prompt's *scope*, not just its model:
      - `Classify` (per-candidate, local) → HTTP backend.
      - `Subcarve` (per-subdir, local) → HTTP backend with map/reduce.
      - `Stage1Surface` (per-component, bounded local) → Claude Code backend, scoped to one component tree.
      - `Stage2Edges` (cross-component, global) → Claude Code backend, full multi-component context. This is the layer that finds protocol/schema/API links and must see surfaces across components together.
    - Configurable via CLI / env for testing, but the defaults encode the design.

    ### Plain HTTP backend

    - New `AnthropicHttpBackend` in `crates/atlas-llm/`. Uses the Anthropic Rust SDK (or raw `reqwest` + `serde_json`). Reads `ANTHROPIC_API_KEY`.
    - **Prompt caching enabled** — system prompt + ontology + component-kinds vocabulary become a `cache_control` block, so the static prefix is paid once per ~5 minutes across all calls.
    - **Structured JSON output** validated against the existing per-prompt JSON schemas in `crates/atlas-cli/src/prompts.rs`.

    ### L8 subcarve map/reduce

    - Replace today's "one agent call asks 'what are the sub-components of X'" with:
      - Map: for each immediate sub-directory of X, build a prompt containing only that subdir's name, listing of top-level entries, and primary manifest snippet (~500 tokens). Fire one HTTP call. Output: `{ is_component, kind, role, evidence }`.
      - Reduce: deterministic — collect verdicts, build the `SubcarvePlan`. No second LLM call.
    - Bounded concurrency executor (start with 8-16 in flight; tune empirically) so 74 map calls parallelise safely.

    ### Cache compatibility

    - Existing `llm-cache.json` is content-addressed — small map prompts produce small, deterministic cache keys, which is what we want. Verify the cache fingerprint includes the *post-truncation* prompt body so behaviour is reproducible.
    - A monorepo with N nearly-identical Rust crates should see most of those N L3 calls collapse to **one cache entry on first run** because the sanitised prompt is byte-equal. Quantify the hit-rate change.

    ### Per-call context budget

    - Each map call has a hard input cap (e.g., 4 KB pre-prompt content). Request builder enforces it deterministically (top-N entries by lexicographic sort, then "this is a sample" suffix). Keeps prompts cheap and reproducible.

    ### Execution / concurrency

    - Add a Tokio runtime in atlas-cli (or use the existing one if Claude Code's backend already drives one) and a bounded `mpsc` / `Semaphore` to gate concurrent HTTP calls.
    - Errors per map-call are isolated: one failure marks one subdir's verdict as `unknown` rather than poisoning the whole subcarve. Plan-assembly degrades gracefully.

    ### Goldens / eval

    - Re-run dev-workspace and the synthetic-second target through the new pipeline. Update goldens only where structural divergence is intentional (e.g., the map-step is more granular than the agent-step would have been).
    - Record before/after wall-time and accuracy in `evaluation/results/`.

    ## Risks and unknowns

    - **Cross-cutting reasoning loss within map calls.** A map-step subagent looking at `crates/foo/src/io/` doesn't see `crates/foo/src/db/`. For L8 per-subdir verdicts this is the right shape; for L3 verify the existing prompt doesn't lean on cross-file context the new shape can't supply. Spike feedback feeds this.
    - **L6 edge-synthesis quality must not regress.** The architectural change narrows L3 and L8 specifically because their answer is local; L6 has to remain *broader*, not narrower. A regression here would mean Atlas misses the cross-component links (shared protocols, schemas, REST endpoints, message-bus topics) that motivate the project. Track L6's `edge_precision` and `edge_recall` against the goldens through the conversion and treat any drop ≥ 2pp as a blocker.
    - **API rate limits.** 74 concurrent calls × multiple iterations could hit per-minute limits on the Anthropic API. Bounded concurrency is the lever; calibrate against published limits.
    - **`llm-cache.json` byte-identity.** With map/reduce the cache file shape may change (smaller entries, more of them). Verify `--dry-run` re-runs are still byte-identical post-conversion.
    - **Mixed-backend `LlmFingerprint`.** Today the fingerprint encodes one backend per run. The router needs to merge two backends' fingerprints into a stable composite, or each `Classification` records the backend used.

    ## Out of scope

    - L5 surface and L6 edges conversion — keep agentic, address in a follow-on if/when needed.
    - Replacing Claude Code itself; `ClaudeCodeBackend` stays for the genuinely exploratory prompts.
    - A user-pluggable backend system (Ollama / OpenAI / etc.) — useful but a larger surface.

    ## Exit criteria

    - `AnthropicHttpBackend` lands and is wired as the default for `Classify` and `Subcarve` prompt ids.
    - L8 subcarve runs in map/reduce mode with bounded concurrency.
    - Real-monorepo first-iteration L8 wall-time **≤ 5 min** (current >>15 min) on the user's target.
    - `kind_accuracy` against the dev-workspace golden is within 2pp of the pre-conversion baseline.
    - `--dry-run` re-run produces byte-identical outputs to the prior run.
    - Documentation: a section in the design spec (`docs/superpowers/specs/`) describing the routing rules, map/reduce shape, and concurrency model.
- id: add-openrouter-as-an-llm-provider
  title: Add OpenRouter as an LLM provider
  category: feature
  status: done
  dependencies: []
  description: |
    **Priority: MEDIUM.** Add OpenRouter (https://openrouter.ai) as a
    selectable LLM provider in `.atlas/config.yaml`, parallel to
    `anthropic/`, `openai/`, `claude-code/`, `codex/`.

    ## Why

    OpenRouter is a routing layer that exposes a single
    OpenAI-Chat-Completions-compatible API across hundreds of models from
    many providers (Anthropic, Google, Mistral, DeepSeek, Meta, etc.). One
    account and one key gets users access to models Atlas otherwise
    couldn't reach without provider-specific HTTP clients. Useful for cost
    optimisation (route Classify to a cheap model) and model
    experimentation without per-provider integration work.

    ## Approach options (decide during implementation)

    1. **Reuse `OpenAiHttpBackend` with a configurable base URL.** OpenRouter
       speaks the OpenAI Chat Completions wire format verbatim. Adding a
       `base_url: Option<String>` parameter to `OpenAiHttpBackend` and a new
       `openrouter/` branch in `router.rs` that constructs it pointed at
       `https://openrouter.ai/api/v1/chat/completions` is the smallest
       diff. OpenRouter also accepts optional `HTTP-Referer` and
       `X-Title` headers for app attribution — surface these as optional
       `params` fields.

    2. **Dedicated `OpenRouterHttpBackend`.** A separate struct that
       composes `OpenAiHttpBackend` internally. Useful if OpenRouter-specific
       features (provider preference routing, fallback chains, transforms)
       need to surface as first-class config. Probably not needed for v1 —
       OpenRouter's OpenAI-compatible mode covers everything Atlas does
       today.

    Recommendation (open to revision): start with option 1.

    ## Scope

    - `crates/atlas-llm/src/http_openai.rs` (or a sibling): make the base
      URL configurable, defaulting to OpenAI's URL when unset.
    - `crates/atlas-llm/src/router.rs`: add `"openrouter"` provider branch
      that constructs the backend with the OpenRouter URL and reads
      `providers.openrouter.api_key` from config.
    - `.atlas/config.yaml` example / docs: show an OpenRouter config
      example. Model strings would look like
      `openrouter/anthropic/claude-sonnet-4-6` — note the second `/` is
      part of OpenRouter's model id, not Atlas's provider/model split.
      This means the existing `model.split_once('/')` logic in
      `router.rs` needs adjustment to take only the *first* segment as
      provider and pass the rest verbatim.
    - Optional headers: `HTTP-Referer` and `X-Title` for OpenRouter's app
      attribution. Surface as `providers.openrouter.referer` /
      `providers.openrouter.app_title` config fields, defaulted off.
    - Tests: parser-level tests for OpenRouter responses (likely identical
      to OpenAI; verify with one fixture). Optionally an `#[ignore]`
      integration test gated by `ATLAS_LLM_RUN_OPENROUTER_TESTS=1` and an
      `OPENROUTER_API_KEY` env var.

    ## Open design questions

    - **Provider/model splitting.** Today `router.rs` uses
      `model.split_once('/')`. With OpenRouter, `openrouter/anthropic/claude-sonnet-4-6`
      needs `splitn(2, '/')` so the rest passes verbatim as the model id.
      Verify no other backend assumes a single `/` in the model string.
    - **Cost/budget tracking.** OpenRouter returns pricing in response
      `usage` data. Atlas's budget sentinel may want to read this rather
      than relying on per-provider token counts.
    - **Caching fingerprint.** OpenRouter routes to backend providers
      opaquely; the same model id can be served by different upstreams.
      `LlmFingerprint::backend_version` should incorporate any provider
      hint OpenRouter returns to keep the Salsa cache sound.

    ## Exit criteria

    - `openrouter/<model-id>` selectable via `.atlas/config.yaml`,
      dispatched by the router, runs end-to-end against a real OpenRouter
      account.
    - Provider/model splitting handles OpenRouter's nested model ids
      without breaking the existing `anthropic/`, `openai/`, `claude-code/`,
      `codex/` paths.
    - `cargo test -p atlas-llm` green; gated integration test passes
      under `ATLAS_LLM_RUN_OPENROUTER_TESTS=1`.
    - README's Usage section mentions OpenRouter as a supported provider.
  results: |
    ## What was done

    Added OpenRouter as a selectable LLM provider in `.atlas/config.yaml`. Followed task option 1 (reuse `OpenAiHttpBackend` with a configurable base URL); option 2 (a dedicated struct) was unnecessary because OpenRouter's wire format is OpenAI-compatible.

    ### Implementation

    - **Configurable upstream** (`crates/atlas-llm/src/http_openai.rs`)
      - Added `pub const OPENAI_DEFAULT_URL`.
      - `OpenAiHttpBackend` gained two fields: `base_url: String` (defaults to `OPENAI_DEFAULT_URL`) and `provider_label: String` (defaults to `"openai"`).
      - New builder method `with_base_url(base_url, provider_label)` overrides both — chained off `new()` to keep existing call sites unchanged.
      - All log/error messages now interpolate `provider_label` so an OpenRouter request that 4xx's says `openrouter API returned 4xx: …` not "OpenAI".
      - Fingerprint's `backend_version` is now `openai-http/<crate-ver>+upstream=<provider-label>`. Two backends pointed at different OpenAI-compatible URLs produce distinct fingerprints — Salsa cache stays sound across upstreams even when the model id collides.
      - Two new unit tests assert the default and overridden labels.

    - **Shared `HTTP_PROVIDERS` constant** (`crates/atlas-llm/src/config.rs`)
      - Promoted the previously-duplicated `HTTP_PROVIDERS` const to module-level `pub const HTTP_PROVIDERS: &[&str] = &["anthropic", "openai", "openrouter"]`.
      - `router.rs` now references `crate::config::HTTP_PROVIDERS` rather than carrying its own copy. Resolves the de-duplication note flagged in memory entry `backendrouter-rejects-http-providers-for-stage1surface-and-stage2edges`.
      - `validate()` now also enforces `api_key` presence and `params.max_tokens` for openrouter operations, exactly like anthropic/openai.

    - **Router branch** (`crates/atlas-llm/src/router.rs`)
      - New `"openrouter"` arm in the provider match: constructs `OpenAiHttpBackend::new(...)` then `.with_base_url("https://openrouter.ai/api/v1/chat/completions", "openrouter")`.
      - Reads `providers.openrouter.api_key` from config.
      - The existing `split_once('/')` already handles OpenRouter's nested model ids correctly — the first `/` separates provider from model id, and `claude-sonnet-4-6` (with its embedded `/`) passes verbatim. A dedicated test (`nested_model_id_splits_only_on_first_slash`) pins this behavior.
      - Rejection message updated to list `(anthropic, openai, openrouter)`.

    - **Init template** (`crates/atlas-cli/src/init.rs`)
      - Documented the openrouter provider with the special case for nested model ids.
      - Added a commented-out `providers.openrouter.api_key` example with `${OPENROUTER_API_KEY}` interpolation.

    - **README** (`README.md`)
      - Added an "LLM providers" subsection to Usage listing all five providers and noting that HTTP providers are restricted to `Classify`/`Subcarve` until the tool-use loop lands.

    ### Tests added (7 new)

    - `http_openai::tests::fingerprint_default_label_is_openai` — asserts default upstream label.
    - `http_openai::tests::fingerprint_label_changes_with_base_url` — asserts custom label appears in fingerprint.
    - `router::tests::rejects_openrouter_for_stage1_surface` — surface prompt rejected.
    - `router::tests::accepts_openrouter_for_classify_and_subcarve` — non-filesystem prompts accepted.
    - `router::tests::nested_model_id_splits_only_on_first_slash` — pins splitting semantics for OpenRouter ids.
    - `router::tests::http_providers_constant_includes_openrouter` — pins shared constant content.
    - `config::tests::load_accepts_openrouter_with_per_op_max_tokens` / `load_rejects_openrouter_without_max_tokens` / `load_rejects_openrouter_missing_provider_entry` — exercise validation parity with anthropic/openai.

    ### Test run

    - `cargo test -p atlas-llm --lib` — **132 passed, 0 failed, 4 ignored** (was 125 → +7 new tests; one earlier router test removed in favour of cleaner constant-check).
    - `cargo test --workspace` — all suites green; full test count unchanged elsewhere.
    - `cargo clippy --workspace --all-targets` — clean.

    ### Exit criteria status

    - ✅ `openrouter/<model-id>` selectable via `.atlas/config.yaml`, dispatched by router. Live OpenRouter integration **not exercised** — would require an API key and OpenRouter account.
    - ✅ Provider/model splitting handles OpenRouter's nested model ids without breaking anthropic/openai/claude-code/codex paths (verified by all existing router tests + new pinning test).
    - ✅ `cargo test -p atlas-llm` green.
    - ❌ Gated integration test under `ATLAS_LLM_RUN_OPENROUTER_TESTS=1` not added — deferred (would require a fixture against a live OpenRouter response, similar pattern to the agent-progress e2e test gated on `ATLAS_LLM_RUN_CLAUDE_TESTS=1`).
    - ✅ README's Usage section mentions OpenRouter alongside other providers.

    ### Deferred

    - **Optional headers `HTTP-Referer` / `X-Title`** for OpenRouter app attribution. Not strictly necessary — OpenRouter accepts requests without them and surfaces "unknown app" in dashboards. Add as `providers.openrouter.referer` / `providers.openrouter.app_title` config fields once a real user hits the dashboard friction.
    - **Gated integration test** against a real OpenRouter account. Pattern would mirror the existing `ATLAS_LLM_RUN_CLAUDE_TESTS=1` gate; needed before claiming end-to-end correctness against the live API.
    - **Cost/budget tracking** from OpenRouter's `usage.cost` field. Atlas's `BudgetSentinel` currently tracks tokens, not dollars. Threading cost through is a separate task.

    ### What this suggests next

    - The shared `HTTP_PROVIDERS` constant is now load-bearing in two places (config validation + router rejection). The filesystem-tool-use-loop task will need to *narrow* the rejection list per-backend rather than removing it entirely — capability flags on individual backends (e.g. `LlmBackend::supports_filesystem()`) would be the natural shape.
    - Memory entry `backendrouter-rejects-http-providers-for-stage1surface-and-stage2edges` mentions `HTTP_PROVIDERS` is "duplicated from `config.rs::validate`'s `HTTP_PROVIDERS` constant; consolidate if a third caller arrives." It is now consolidated; the memory entry should be updated to reflect the new shared-constant location.
- id: run-agent-progress-e2e-integration-test-against-real-claude-binary
  title: Run agent-progress e2e integration test against real claude binary
  category: eval
  status: not_started
  dependencies: []
  description: |
    **Priority: LOW.** The agent-progress e2e integration test (`cargo test -p atlas-cli --test agent_observer_e2e`) skips silently by default. Running it once with `ATLAS_LLM_RUN_CLAUDE_TESTS=1` would close the loop on the full streaming pipeline: `ClaudeCodeBackend` → `parse_stream` → `AgentObserver` → `Reporter` agent sub-line.

    ## What to do

    1. Run: `ATLAS_LLM_RUN_CLAUDE_TESTS=1 cargo test -p atlas-cli --test agent_observer_e2e -- --nocapture`
    2. Verify the test passes end-to-end.
    3. If it fails, investigate and fix before marking done.
    4. Estimated cost: ~$0.05 USD, ~30–90 s.

    ## Context

    The agent sub-line feature (shipped in the prior cycle) is verified only by parser-level fixture tests and `Reporter` rendering unit tests. The full integration path (real `claude -p --output-format stream-json --verbose` invocation → JSONL parse → `AgentEvent` emission → `Reporter` sub-line mount/refresh/unmount) has not been exercised end-to-end since implementation.

    ## Exit criteria

    - Test passes under `ATLAS_LLM_RUN_CLAUDE_TESTS=1`.
    - No regressions in other `atlas-cli` tests.
- id: filesystem-tool-use-loop-for-http-llm-backends-provider-agnostic
  title: Filesystem tool-use loop for HTTP LLM backends (provider-agnostic)
  category: feature
  status: not_started
  dependencies: []
  description: |
    **Priority: MEDIUM.** Add a generic filesystem-tool-use loop to `AnthropicHttpBackend` and `OpenAiHttpBackend` so HTTP providers can service `Stage1Surface` and `Stage2Edges`. Once landed, the routing rejection in `BackendRouter` (added in `reject-http-providers-for-stage1surface-and-stage2edges-in-backendrouter`) loosens to a per-backend capability check rather than an unconditional ban on `provider in {anthropic, openai}`.

    ## Context

    Today, `Stage1Surface` and `Stage2Edges` only run on subprocess backends (`claude-code`, `codex`) because their rendered prompts ship no file content — surface and edge extraction need to read source. Subprocess backends provide that filesystem access via the upstream agent's built-in tools (Read, Grep, Glob, Bash). HTTP backends carry only the rendered prompt text, so they cannot read files and must currently be rejected for these prompt ids.

    Both Anthropic Messages API and OpenAI Chat Completions support **tool use / function calling** as a first-class part of the wire protocol. With a tool-use loop driven by Atlas, an HTTP backend can delegate filesystem reads back to the host process and produce surfaces/edges with the same fidelity as a subprocess backend — at lower cost (HTTP is cheaper per call than full agent sessions), with prompt caching benefits, and against any provider that speaks the protocol (Anthropic, OpenAI, OpenRouter, Vertex, Bedrock, etc.).

    User-stated motivation: "We could use the http backends if we provided the tools. I think it could be generally useful, not only here but for other projects as well." Design with reusability in mind — the tool-use loop should be liftable to a separate crate (e.g. `linkuistics/llm-tools` or `atlas-llm-tools`) once it has two consumers.

    ## Scope

    ### Tool set (initial)

    Minimum viable filesystem tool surface, mirroring what the agentic backends use:

    - `read_file(path: str, max_bytes: int = 64KiB) -> { content: str, truncated: bool }`
    - `glob(pattern: str, root: str = "<workspace>") -> { paths: [str] }`
    - `grep(pattern: str, paths: [str] | str, max_matches: int = 100) -> { matches: [{path, line, text}] }`
    - `list_dir(path: str) -> { entries: [{name, kind: "file" | "dir"}] }`

    Each tool's input/output JSON schemas live in one place and are exposed in both Anthropic and OpenAI tool-definition shapes via small adapter functions.

    ### Loop semantics

    1. Initial request: prompt + `tools: [...]` definitions.
    2. Receive response. If `stop_reason: "tool_use"` (Anthropic) or `finish_reason: "tool_calls"` (OpenAI), execute each tool call against the host filesystem, append `tool_result` / `tool` messages, send next turn.
    3. Repeat until `stop_reason: "end_turn"` or `finish_reason: "stop"`, or until a hard turn cap (e.g. 25) trips an `LlmError::Invocation`.
    4. Final assistant text is the structured-JSON response — same validation path as today via existing `validate_response`.

    ### Sandboxing

    Tool implementations must restrict filesystem access to a configured root (the Atlas workspace path). Path normalisation rejects `..` escapes and absolute paths outside the root. No write tools — read-only. `grep` and `glob` re-use Atlas's existing `ignore`-crate-aware traversal so `.gitignore` filtering applies (subprocess backends already inherit this from the agent host's defaults).

    ### Configurable budget

    - `max_turns` (default 25): maximum back-and-forth iterations per call.
    - `max_tool_calls_per_turn` (default 10): caps fan-out within one turn.
    - `max_total_bytes_read` (default 1 MiB per call): caps cumulative bytes returned via `read_file` across the whole loop.

    Exhausting any cap returns `LlmError::Invocation` with a clear message; the call is recorded as an error in the existing budget/sentinel stack.

    ### Provider adapters

    - **AnthropicHttpBackend.** Tools are sent under `tools: [{name, description, input_schema}, ...]`; tool results returned as `content: [{type: "tool_result", tool_use_id, content: <json>}]` blocks in the next user message.
    - **OpenAiHttpBackend.** Tools sent under `tools: [{type: "function", function: {name, description, parameters}}, ...]`; tool results returned as `{role: "tool", tool_call_id, content: <json-string>}` messages.
    - Wire-format differences absorbed by per-provider adapters; the engine-facing surface (the `LlmBackend::call` impl) is unchanged.

    ### Reusability shape

    Aim for a clean internal split so the tool-use loop and the tool implementations can be lifted to a sibling crate later:

    - `tool_use::loop::run<Provider, Tools>(provider: &Provider, tools: &Tools, messages, budget) -> Result<String>` — generic over provider adapter and tool implementer traits.
    - `tool_use::filesystem::FilesystemTools` — the concrete read-only filesystem tool set.
    - Atlas's HTTP backends become thin wrappers: parse Atlas's `LlmRequest`, build messages, call `tool_use::loop::run`, validate the final JSON response.

    Don't over-engineer — keep it inline in `atlas-llm` until a second consumer materialises, but design the module boundary so extraction is mechanical.

    ### Cache fingerprint

    `LlmFingerprint::backend_version` must encode "tool-use enabled" + tool-set version (a SHA over the tool definitions). Two runs with different tool sets are not interchangeable; the response cache must invalidate.

    ### Routing relaxation

    Once landed, `reject_http_for_filesystem_required_prompt` in `crates/atlas-llm/src/router.rs` becomes a per-backend capability check. Each backend declares whether it supplies filesystem tools (`fn supports_filesystem_tools(&self) -> bool`); the router asks the resolved backend instead of hard-coding HTTP-provider names.

    ## Cross-references

    - `enable-anthropic-prompt-caching-in-anthropichttpbackend` — caching applies to the static prefix (system + tool definitions); compose the two features so cached prefix benefits stack across tool-use turns.
    - `add-openrouter-as-an-llm-provider` — OpenRouter speaks OpenAI tool-call format verbatim; the OpenAI adapter covers OpenRouter for free.
    - `map-reduce-llm-architecture-with-backend-routing-for-context-light-analysis` — Stage1Surface stays "needs filesystem access" in that design; tool-use HTTP backends become a third option alongside `claude-code` and `codex`.
    - Memory: `backendrouter-rejects-http-providers-for-stage1surface-and-stage2edges` — the rejection's "future relaxation" path points at this task.

    ## Risks

    - **Determinism.** Tool-use loops can take different code paths across runs (the model may pick different files). Cache key remains canonical-input-based; cached-response soundness depends on the fingerprint encoding "tools enabled". Document this; consider a stricter fingerprint that includes tool-set hash.
    - **Cost.** Each round-trip adds prompt + response tokens. A surface extraction that took one agent call may take 5–10 HTTP turns. Budget caps are essential; profile against the dev-workspace before promoting from experimental.
    - **Sandboxing soundness.** Path-traversal escapes via symlinks, `..`, Unicode normalisation, etc. Use `Path::canonicalize` against the workspace root; reject anything outside.
    - **Streaming.** v1 implementation can be non-streaming (`reqwest::blocking`) like the current HTTP backends. Streaming tool-use is a follow-up.

    ## Out of scope

    - Write tools (Edit, Write, Bash). Atlas is read-only against the target codebase by design.
    - Streaming. Block-mode parity with current HTTP backends.
    - Lifting to a separate crate. Design for it; defer until two consumers.

    ## Exit criteria

    - `AnthropicHttpBackend::call` and `OpenAiHttpBackend::call` execute tool-use loops when configured for `Stage1Surface` or `Stage2Edges`.
    - Read-only filesystem tools available: `read_file`, `glob`, `grep`, `list_dir`. Sandboxed to a configured root.
    - `LlmBackend::supports_filesystem_tools(&self) -> bool` declared; default `false` for current backends, `true` for the new tool-using HTTP path; routing layer asks the trait method instead of hard-coding providers.
    - Configuration: `.atlas/config.yaml` `params.tools.enabled: true` (or similar) opts a per-operation HTTP backend into tool-use mode. Default off so existing configs are unchanged.
    - Cache fingerprint encodes "tools enabled" + tool-set hash; existing zero-LLM-call no-op re-runs remain byte-identical.
    - Live end-to-end: `atlas index ~/Development/dull/` with `surface` routed to `anthropic/...` (tools enabled) produces non-empty surfaces and edge proposals; metrics within 5pp of the same run on `claude-code/...`.
    - Memory note added documenting the design and the routing-relaxation that follows.
